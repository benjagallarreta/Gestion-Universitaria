public class PlanB implements StrategyPlanEstudio {
    
    @Override
    public class verificarCondicion (Materia materia, Alumno alumno) {

        for(materia.correlativas) {
            if (materia.correlativas(i).estado == aprobado) {
                
            }
        }

    }

}
